services:
  - type: web
    name: tamir-lisans-api
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: LISANS_GIZLI_ANAHTAR
        generateValue: true
      - key: ADMIN_API_KEY
        generateValue: true
